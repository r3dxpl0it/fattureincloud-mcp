# Runtime dependencies for the MCPB bundle.
#
# Upper bounds are deliberate. Until v2.1.0 these were unbounded (`mcp>=1.0.0`),
# which meant a build done today resolved mcp 2.x - whose Server class dropped
# the @list_tools/@call_tool decorators this server is written against - and
# produced a bundle that could not even import. Reproducible builds use
# requirements.lock; this file records the supported ranges.
fattureincloud-python-sdk>=2.1.3,<3
mcp>=1.27,<2
